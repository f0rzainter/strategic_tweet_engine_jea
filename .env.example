# Copy this file to `.env` in the same folder as `app.py` and set your values.
# Get an API key: https://platform.openai.com/api-keys
#
# Example (not a real key — replace with your own):
OPENAI_API_KEY=sk-fake-placeholder-replace-with-your-openai-key
